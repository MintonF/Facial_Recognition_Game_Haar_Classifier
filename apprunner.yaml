version: "1.0"
service:
  name: "facial-recognition-game-haar-classifier"
  runtime: python3.9 # Or the correct, supported runtime
  build:
    dockerfile: Dockerfile
  ports:
    - protocol: tcp
      port: 5000















